now:

1. notifications should also be localised // 🛑 high priority
2. further clientsite customization : fonts (🟡 mid priority), background shapes/patterns (🟡 mid priority), more color variety (🛑 high priority)
3. misc style/ui cleanups OR general code optimisation
4. centralised ArticleModal component (for ArticleCreate/Edit) // low priority 🟢
5. HP/index page should get a UI rework and move from MVP to actual production ready page // 🟡 mid priority
6. fix Comment/Section - sorting options not working, refresh issues (duplicate articles, reactions) // 🟡 mid priority
7. implement ModalMini (exp. ArticleCreate draft select could use it instead of sweetalert) // low priority 🟢
---

once deployed:

1. GAM + total ad revenue overview & logging // 🛑 high priority
2. nuxt-gtag // 🛑 high priority
3. ai sentiment analysis based on comments. (post comment -> onCommentCreated (ai reviews comments, rates it) -> aggregation of all comments and result community/reader perception of articles) - PREMIUM function // 🔰 low-mid priority
4. ai cron jobs for article writing // 🛑 high priority
5. automate onboarding flow : wp form -> post fetch on topiqu backend -> vercel api -> create subdomain/return custom domain dns details // 🟡 mid priority
6. test // 🛑 high priority

---

future:

1. bullmq / railway / queue up cron jobs // low priority 🟢
2. better gamification - badges, levels, site activity points
(exp. 100 comments reached, 25 articles liked, following 10 authors, positive karma (total likes - total dislikes))... // 🔰 low-mid priority

---

done:

1. cliensite language column // 🛑 high priority
2. cliensite customization (themes) // 🛑 high priority
3. add sessionId for oauth providers in nuxt auth callback
4. account health - sessions, password strength, 2fa, amount of bans, verified email... // 🟡 mid priority
5. fix header Z-index
6. fix background floating objects z-index
7. user page - enhance/improve styling & ui for hero section
8. lang switcher -> improve styles
9. useClientSite composable - return language/cliensite details based on host header // 🛑 high priority
10. all clientsite endpoints should use host header instead of clientsite slug/route params // 🛑 high priority
11. rework related articles logic - should have fallback when no tags are present, and select from more tags, not only one // low priority 🟢
12. implement missing email templates // 🛑 high priority
