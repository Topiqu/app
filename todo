always:
  - 🛑 keep misc style/ui clean
  - 🛑 keep code optimised
  - 🛑 test out before marking done
---
now:
  - 🛑 root Feed page na apex (topiqu.com) — "best of" články napříč všemi tenants; nahradí prod redirect apexu na landing.topiqu.com (app.vue#isLandingSurface), který je dočasný než Feed vznikne
  - 🛑 GAM + total ad revenue overview & logging
  - 🛑 AWS Comprehend integration
  - 🛑 dynamic pricing (clientpreferences)
  - 🛑 move Stripe / AWS / xAI / Upstash / LinkedIn / Giphy from NW to personal acc
  - 🛑 Sentry/Better Stack: add keys to Dokploy env (build args/secrets — SENTRY_AUTH_TOKEN must be a Build Secret, not a Build Arg) + CI secret (NUXT_PUBLIC_SENTRY_DSN + SENTRY_URL/ORG/PROJECT/AUTH_TOKEN ; BETTERSTACK_SOURCE_TOKEN=2qYuohMH4XKWEgi9MeJczhS6 + BETTERSTACK_INGEST_HOST=s2479774.eu-fsn-3.betterstackdata.com)
  - 🛑 Better Stack: UNBLOCKED (live domain since the Dokploy cutover) — enable uptime monitor on app.topiqu.com/api/health, keyword "status":"ok" (NOT topiqu.com — the apex is the separate landing project and has no /api/health) + create heartbeats for all SIX tasks now that they actually fire natively (publish-check 10m / generate-article daily / sentiment-analysis 30m / community-insights daily / translate-pending 5m / gam-sync daily), then put ping URLs in BETTERSTACK_HEARTBEAT_* env
  - 🟡 Better Stack: BLOCKED on heartbeat URLs — wire withHeartbeat() into the 4 server/tasks (helper ready in server/utils/monitoring.ts, no-op without env)
  - 🟡 Stripe integration for token refill
  - 🟡 AI Inolvement with article (cron based = fully AI, AI generated/enhanced = partially AI, no AI used = 100% human)
  - 🟢 replace sweetalert modals with custom ModalMini component
  - 🟢 implement in clientpreferences humanHourlyRate / humanWordsPerHour
  - 🛑 cache: code DONE (cache.ts on ioredis/REDIS_URL, @upstash/redis removed, invalidateFeed wired into create/patch/delete/publish-check/generate-article, TTL 60s->10min, 13 tests, verified against a real Valkey). REMAINING = ops only: add the valkey service to the Dokploy stack (valkey/valkey:8.1-alpine, --maxmemory 256mb --maxmemory-policy volatile-lru --save "", port NOT published) and set REDIS_URL in Dokploy env. Until that is done prod still runs with caching disabled — cache.ts warns "[cache] REDIS_URL not set" once at startup
  - 🛑 verify APP_ENV in the running container — nuxt.config.ts:3 is APP_ENV || VERCEL_ENV || NODE_ENV || 'development'; VERCEL_ENV is gone and the Dockerfile sets NODE_ENV=production only in the prerelease stage, which release (FROM base) does not inherit. If it resolved to 'development' we're paying 5× Sentry traces (1.0 vs 0.2), losing session replays, and tagging prod errors as environment=development. Check: docker exec <c> printenv | grep -E "APP_ENV|NODE_ENV"
  - 🛑 delete vercel.json — dead weight since cutover; if the manifest.webmanifest / sitemap.xml content-type overrides still matter, re-express them as nitro routeRules first
  - 🟡 document the wildcard TLS mechanism for *.topiqu.com in MAP.md §4 (Cloudflare Origin CA / DNS-01 / tunnel?) — it works, but nothing records how
  - 🟡 per-account login lockout (was BLOCKED on KV/Redis — unblocks as soon as Valkey lands) — email-keyed exp. backoff + Turnstile on auth/callback/credentials + users/totp; per-IP rate-limit alone is botnet/proxy-bypassable
---
test:
  - 🛑 google ads
  - 🛑 google analytics
  - 🛑 Sentry/Better Stack: verify event + session replay delivery (test throw on FE + Nitro)
  - 🛑 OAuth ?redirect param
  - 🛑 fix auth errors with zod id param
  - 🛑 notifications should also be localised
  - 🛑 production fix email template json not loadings
  - 🛑 ai cron jobs for article writing and its notifications
  - 🛑 semantic article dedupe (make sure cronjobs dont repeat the same topic all over again + integrate sentiment suggestion)
  - 🟡 Localise notifications used in crons/nitro-tasks (think up how to detect language since tasks dont provide event)
  - 🟡 fix prod mjml parse issue ([request error] [unhandled] [POST] https://johnnyho-blog.topiqu.com/api/comments
          H3Error: Parsing failed. Check your mjml.
              at MJMLParser (/var/task/node_modules/mjml-parser-xml/lib/index.js:286:11)
              ... 7 lines matching cause stack trace ...
              at async Server.<anonymous> (/opt/rust/nodejs.js:16:7750) {
            cause: Error: Parsing failed. Check your mjml.
                at MJMLParser (/var/task/node_modules/mjml-parser-xml/lib/index.js:286:11)
                at mjml2html (/var/task/node_modules/mjml-core/lib/index.js:186:39)
                at sendEmail (file:///var/task/chunks/nitro/nitro.mjs:7119:28)
                at async Object.handler (file:///var/task/chunks/routes/api/index.post3.mjs:125:5)
                at async file:///var/task/chunks/nitro/nitro.mjs:2452:19
                at async Object.callAsync (file:///var/task/chunks/nitro/nitro.mjs:5838:16)
                at async toNodeHandle (file:///var/task/chunks/nitro/nitro.mjs:2723:7)
                at async Server.r (/opt/rust/nodejs.js:2:15569)
                at async Server.<anonymous> (/opt/rust/nodejs.js:16:7750),
})
  - 🟡 fix server i18n not loading correctly dynamic languages
  - 🟡 Localise user PDF export
  - 🟢 centralised ArticleModal component (for ArticleCreate/Edit)
  - 🟢 remake ArticleModal Drafts with ModalMini
---
optional:
  - deps to upgrade
    - 🛑 @sidebase/nuxt-auth 1.2.0 -> 1.3.1 — pulls @auth/core 0.41.3, fixes critical auth bypass GHSA-7rqj-j65f-68wh (exact-pinned, bun update won't do it)
    - 🟡 @pinia/nuxt 0.11.3 -> 1.0.1
    - 🟢 ua-parser-js -> 2.0.10
    - 🔰 @prisma/client 6 -> 7 — NO
  - 🟡 gemini-3.1-pro-preview is a preview id (server/utils/ai/modelRegistry.ts) — recheck before GA/retirement
  - further clientsite customization
    - 🛑 more color variety
    - 🟡 background shapes/patterns
    - 🟡 fonts
  - 🟡 HP/index page should get a UI rework and move from MVP to actual production ready page
  - 🟢 article series (how to build a house 1/5) + next part/back button
  - 🟢 feedback loop (was this helpful ? 👍/👎)
  - 🟢 favorite button for articles (bookmark/save)
  - 🟢 tiptap for comments
---
future:
  - 🛑 zapier integration
  - 🟡 automate onboarding flow: wp form -> post fetch on topiqu backend -> vercel api -> create subdomain/return custom domain dns details
  - 🔰 (exp. 100 comments reached, 25 articles liked, following 10 authors, positive karma (total likes - total dislikes))...
  - 🔰 newsletter for pro+ clients
  - 🔰 multi agent writing (seo analyzer, copywriter, reviewer) with tools (debate, vote, merge, post)
  - 🟢 better gamification - badges, levels, site activity points
---
queue (SQS migration):
  - ✅ serverless → self-hosted migration DONE (Dokploy/VPS, 2026-08) — MIGRATION.md deleted, runtime facts now in MAP.md §1/§4/§8
  - 🛑 decide: 1 web replica vs N — BLOCKING before anyone scales web in Dokploy: all 6 nitro scheduledTasks now fire natively PER REPLICA, so 2 replicas = 2 generated articles + double-counted gam-sync revenue. Options: pin web=1 / move tasks to single-replica worker / swap for Dokploy Schedule Jobs + CRON_SECRET endpoint
  - 🛑 decide: queue in Redis Streams vs Postgres SKIP LOCKED — ArticleJob/PdfJob are Prisma models and PG is already managed+backed up; Streams adds a second stateful system and forces persistence config on the cache instance. Decide before writing queue.ts
  - 🛑 server/utils/queue.ts abstraction (SQS + in-memory) + LocalStack in docker-compose
  - 🛑 Prisma models: ArticleJob, PdfJob
  - 🛑 server/worker.ts entrypoint (4 paralelní pollery)
  - 🛑 SQS queues + DLQ + IAM (infra/CDK)
  - 🟡 migrate POST /articles/generate → enqueue (202 + jobId)
  - 🟡 cron generate-article → fan-out only (enqueue N msgs)
  - 🟡 split notifications worker (DB createMany + SSE publish)
  - 🟡 unified SSE server-events channel (notifs + job status, cross-replica)
  - 🟡 useJobStream composable + UI loading/error/retry states
  - 🟡 GET /users/pdf → enqueue + pdf-worker (S3 upload + signed URL)
  - 🟡 view counter: enqueue + 5min batch UPDATE worker
  - 🟢 CloudWatch alarms (queue depth, DLQ > 0, retry 3× exp backoff)
  - 🟢 vitest workers (in-memory queue) + LocalStack in CI + MAP.md
---
done:
  - 🛑 nuxt-gtag
  - 🛑 cliensite language column
  - 🛑 cliensite customization (themes)
  - 🛑 implement missing email templates
  - 🛑 add sessionId for oauth providers in nuxt auth callback
  - 🛑 useClientSite composable - return language/cliensite details based on host header
  - 🛑 all clientsite endpoints should use host header instead of clientsite slug/route params
  - 🛑 AWS S3 Bucket integration
  - 🛑 AWS SES integration
  - 🛑 AWS Rekognition integration
  - 🛑 fix issue in users/[id]/patch : {
  "error": true,
  "url": "http://localhost:3000/api/users/f0ddd9fb-42af-4fe9-ab4e-4dd9257b632a",
  "statusCode": 400,
  "statusMessage": "Validation Error",
  "message": "Invalid element at key \"id\": expected a Zod schema",
  "data": {},
  "stack": [
    "Invalid element at key \"id\": expected a Zod schema",
    "at createError (C:/Users/kurib/rasg-blog/node_modules/h3/dist/index.mjs:71:15)",
    "at createValidationError (C:/Users/kurib/rasg-blog/node_modules/h3/dist/index.mjs:237:9)",
    "at validateData (C:/Users/kurib/rasg-blog/node_modules/h3/dist/index.mjs:233:11)",
    "at readValidatedBody (C:/Users/kurib/rasg-blog/node_modules/h3/dist/index.mjs:454:10)",
    "at process.processTicksAndRejections (node:internal/process/task_queues:105:5)",
    "at async Object.handler (C:\\Users\\kurib\\rasg-blog\\server\\api\\users\\[id]\\index.patch.ts:17:0)",
    "at async file:///C:/Users/kurib/rasg-blog/node_modules/h3/dist/index.mjs:2004:19)",
    "at async Object.callAsync (C:/Users/kurib/rasg-blog/node_modules/unctx/dist/index.mjs:72:16)",
    "at async Object.callAsync (C:/Users/kurib/rasg-blog/node_modules/unctx/dist/index.mjs:72:16)",
    "at async Server.toNodeHandle (C:/Users/kurib/rasg-blog/node_modules/h3/dist/index.mjs:2296:7)"
  ]
}
  - 🟡 autorelease articles
  - 🟡 encapsulate article exports
  - 🟡 create dropdown menu component w groups
  - 🟡 account health - sessions, password strength, 2fa, amount of bans, verified email...
  - 🟡 footer branding for free tier clients
  - 🟡 fix Comment/Section - sorting options not working, refresh issues (duplicate articles, reactions)
  - 🔰 ai sentiment analysis based on comments. (post comment -> onCommentCreated (ai reviews comments, rates it) -> aggregation of all comments and result community/reader perception of articles) - PREMIUM function
  - 🟢 fix header z-index
  - 🟢 lang switcher -> improve styles
  - 🟢 fix background floating objects z-index
  - 🟢 user page - enhance/improve styling & ui for hero section
  - 🟢 rework related articles logic - should have fallback when no tags are present, and select from more tags, not only one
  - 🔰 generic canary / feature-flag primitive (modular + scalable) - reusable, not cache-specific. Best practices:
      * deterministic bucketing hash(subject + flagKey) % 100 < rollout (NOT Math.random) -> sticky per subject, no flapping; salt by flagKey
      * SSR/client consistency: same deterministic hash from a stable subject so server & client agree (avoids hydration mismatch when a flag affects render)
      * config separate from code & changeable at runtime -> rollout state in Redis (Upstash), env/in-code default as fallback chain (Redis -> env -> default OFF); kill switch field for instant rollback, no redeploy
      * evaluate() returns { enabled, variant, reason: KILL_SWITCH|DENY|ALLOW|PERCENTAGE|DEFAULT } not bool, for observability
      * default-safe: unknown flag / provider error -> safe default (OFF for risky new paths); cache outage must not flip a flag on
      * pluggable FlagProvider (swap Env/Redis -> LaunchDarkly/OpenFeature later w/o touching call sites); pluggable subject dimension (requestId/sessionId/userId/clientSiteId; per-tenant rollout is the multi-tenant win)
      * support weighted multivariant (A/B/C), allow/deny lists
      * layout: shared/canary/{types,evaluate}.ts (pure deterministic core, FNV-1a, unit-tested, shared client+server) ; server/utils/canary.ts (provider + 30s in-memory config cache so we don't hit Redis per request + canary() + withShadow()) ; app/composables/useCanary.ts (client eval over SSR-hydrated flags)
      * withShadow(flag, subject, { primary, candidate }) -> runs both, returns primary, logs diff = generic version of the cached-vs-live leak check
      * observability: emit each evaluation (flag, variant, reason, hashed subject) to logs AND Sentry (breadcrumb/tag or measurement) so we can correlate canary cohort vs rest, and a bad variant surfaces in error tracking
      * first consumer: wrap the feed-cache (server/api/articles/by-clientsite/[slug].ts) so canary actually drives the cache we added, not theory
  - 🔰 server cache follow-ups - DONE: temp [cache] HIT/MISS logs dropped, bumpGen wired into article mutations via invalidateFeed(). STILL OPEN: broaden anon-only -> all non-privileged readers; size TTLs from real hit rates (INFO commandstats on the valkey container now that Upstash's request counter is gone).
  - 🟡 cache step 2 (agreed, not started): mount valkey as nitro storage (nitro.storage.cache redis driver) — also fixes ogImage.runtimeCacheStorage running on per-instance memory; then defineCachedEventHandler on the session-free routes, best candidate clients/slug/[slug].ts (raw prisma, publicClientSiteSelect, hit on every SSR render), then gifs/index.get.ts (Giphy quota). ALWAYS write getKey explicitly — the default key comes from the URL and any host-resolved tenant route would collide. Blocked on the APP_ENV fix: clients/slug/[slug].ts branches its where-clause on NODE_ENV, so caching it while prod evaluates as dev would cache the dev matcher.
  - 🟡 cache step 3 (agreed, not started): featured/[slug].ts onto cached() with an anon guard mirroring by-clientsite (it calls getEnhancedPrisma(user), so the payload is role-dependent and must not be cached for logged-in callers)
